FROM python:3.12-slim
WORKDIR /app
COPY pyproject.toml ./
COPY src ./src
RUN pip install --no-cache-dir .
COPY tests /checks
ENV PRICING_TEST_PAYLOAD=/payload
CMD ["python", "-m", "unittest", "discover", "-s", "/checks", "-v"]
